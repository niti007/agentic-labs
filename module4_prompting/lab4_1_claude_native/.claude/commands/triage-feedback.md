---
description: Sort the feedback inbox by urgency using our written criteria + examples
---

Sort customer feedback by urgency, using our agreed rules and format.

1. Read the urgency rules in `criteria.md`.
2. Read the worked examples in `examples.md` — match their exact one-line format and calm tone.
3. Classify each message in `feedback_inbox.md` (or the messages the user pastes below).

For every message, output exactly one line:
`SEVERITY | one-line reason | suggested first action`

Apply the *spirit* of the rules to anything not spelled out (e.g. a privacy/security
problem counts as CRITICAL even if the wording is new). When a message could be two
levels, choose the higher one.

Messages to sort: $ARGUMENTS
